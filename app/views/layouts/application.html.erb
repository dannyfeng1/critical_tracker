<!DOCTYPE html>
<html>
  <head>
    <title>CriticalTracker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans&display=swap" rel="stylesheet">
    <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>

    <link rel="icon" type="image/png" href="https://cdn0.iconfinder.com/data/icons/esports-wildberry-vol-1/256/Critical_Damage-512.png"/>
    
    <meta property="og:title" content="Critical Tracker" />
    <meta property='og:image' content="<%= image_url('crit_tracker_preview.png') %>"/>
    <meta property="og:description"
       content="Critical Tracker offers users the ability to create projects, assemble teams and post stories to keep track of all the rapid changes in a software's development cycle."
    />
    <meta property="og:url"
       content="https://critical-tracker.herokuapp.com/"
    />

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
        <% if current_user %>
      <script> 
        window.currentUser = {
          "id": "<%= current_user.id %>",
          "email": "<%= current_user.email %>",
          "username": "<%= current_user.username %>",
          "firstName": "<%= current_user.first_name %>",
          "lastName": "<%= current_user.last_name %>"
        };
      </script>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
